// Init state
const initialState = {
  nodes: [],
};

const NodeReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};

export default NodeReducer;
